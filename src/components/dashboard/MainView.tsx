import TotalCard from "./card/TotalCard";

const MainView = () => {
    return (
        <main className="p-5">
            <h1>here will be tour finances</h1>
            <TotalCard />
        </main>
    );
};

export default MainView;
